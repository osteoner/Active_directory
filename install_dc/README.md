USE `sconfig` to change:
		Hostname
		change ip adress to static Ip address
		change dns to our own ip address

Install AD Windows feature:
		``` shell
		Install-WindowsFeature AD-Domain-Services -IncludeManagementTools```